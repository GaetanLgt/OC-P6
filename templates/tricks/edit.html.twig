{% extends 'base.html.twig' %}

{% block body %}
    <div class="edit">
        {{ form_start(form) }}
            {{ form_widget(form) }}
            <input type="submit" value="Edit" />
        {{ form_end(form) }}
    </div>
{% endblock %}